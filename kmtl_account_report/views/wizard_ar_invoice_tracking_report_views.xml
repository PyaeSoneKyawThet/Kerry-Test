<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_wizard_ar_invoice_tracking_report_form" model="ir.ui.view">
        <field name="name">kmtl.ar.invoice.tracking.report.wizard.form</field>
        <field name="model">kmtl.ar.invoice.tracking.report</field>
        <field name="arch" type="xml">
            <form string="AR Invoice Tracking Report">
                <sheet>
                    <group>
                        <field name="date_from"/>
                        <field name="date_to"/>
                    </group>
                    <group>
                        <field name="staff_location_ids" widget="many2many_tags"/>
                        <field name="partner_ids" widget="many2many_tags"/>
                        <field name="payment_term_ids" widget="many2many_tags"/>
                        <field name="state_ids" widget="many2many_tags"/>
                        <field name="currency_ids" widget="many2many_tags"/>
                        <field name="account_ids" widget="many2many_tags"/>
                    </group>
                    <footer>
                        <button string="Export XLSX" type="object" name="action_export_xlsx" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_ar_invoice_tracking_report_wizard" model="ir.actions.act_window">
        <field name="name">AR Invoice Tracking Report</field>
        <field name="res_model">kmtl.ar.invoice.tracking.report</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_wizard_ar_invoice_tracking_report_form"/>
        <field name="target">new</field>
    </record>

    <menuitem id="menu_kmtl_ar_invoice_tracking_report"
              name="AR Invoice Tracking Report"
              parent="menu_kmtl_reports_root"
              action="action_ar_invoice_tracking_report_wizard"/>
</odoo>
